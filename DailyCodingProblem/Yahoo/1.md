# Yahoo ![Difficulty](https://img.shields.io/badge/-MEDIUM-yellow)
	
Write an algorithm that computes the reversal of a directed graph. For example, if a graph consists of `A` -&gt; `B` -&gt; `C`, it should become `A &lt;- B &lt;- C`.
	
